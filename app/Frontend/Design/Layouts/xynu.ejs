<!DOCTYPE html>
<html>
    <head>
        <% // EJS logic
            var BIRD3 = require("BIRD3/Support/GlobalConfig");
            var $meta_desc = "The Dragon's Inn is a cozy place for furry and non-furry"
                ." roleplayers as well as casual chatters."
                ." Stop by and hang out with artists and freaks! =)";
            var $pageTitle = BIRD3.config.name+": Error";
            var $cdn = BIRD3.config.CDN.baseUrl;
        %>

        <title><%=$pageTitle%></title>

        <!-- casual -->
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <meta name="description" content="<%=$meta_desc%>"/>
        <!-- Facebook -->
        <meta property="og:title" content="<%=$pageTitle%>"/>
        <meta property="og:type" content="<%=$og_type%>"/>
        <meta property="og:image" content="<%=$og_image%>"/>
        <meta property="og:url" content="<%=req.url%>"/>
        <meta property="og:description" content="<%=$meta_desc%>"/>
        <meta property="fb:admins" content="SexyXynu"/>
        <!-- Twitter -->
        <meta name="twitter:card" content="summary"/>
        <meta name="twitter:url" content="<%=req.url%>"/>
        <meta name="twitter:title" content="<%=$pageTitle%>"/>
        <meta name="twitter:description" content="<%=$meta_desc%>"/>
        <meta name="twitter:image" content="website"/>

        <!-- Now for the fun part. -->
        <%
            //Yii::app()->cdn->css("normalize.css");
            //$base = Yii::app()->cdn->baseUrl."/theme/images";
            // Need to import WebPack scripts.
            /*
                Files required:
                - HASH-xynu.css
            */
        %>
    </head>
    <body>
        <div id="page">
            <!-- I need a way to check if user is dev or not. -->
            <h1><%=e.message%></h1>
            <pre><%=e.stack%></pre>
        </div>
    </body>
</html>
